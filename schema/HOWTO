
* How to create the spacewalk schema:
cd spacewalk
make spacewalk-clean spacewalk-release TBS=DATA_TBS SQLUSER=spacewalk/spacewalk@xe

* How to build the spacewalk schema rpm:
cd spacewalk
make srpm
rpmbuild --rebuild *src.rpm

