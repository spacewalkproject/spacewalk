prefix.collision=12
prefix.comma_separated=every,good,boy,does,fine
prefix.comma_no_trim=every, good , boy , does,fine
prefix.array_one_element=some value
prefix.foo = thirty-three
# define a boolean value in rhn_prefix.conf, call getBoolean.
# Test true, false, 1, 0, y, n, foo, 10
prefix.boolean_true = true
prefix.boolean_false = false
prefix.boolean_1 = 1
prefix.boolean_0 = 0
prefix.boolean_y = y
prefix.boolean_Y = Y
prefix.boolean_n = n
prefix.boolean_yes = yes
prefix.boolean_no = no
prefix.boolean_on = on
prefix.boolean_off = off
prefix.boolean_foo = foo
prefix.boolean_10 = 10
prefix.boolean_empty =
prefix.boolean_on = on
prefix.boolean_off = off
# define an integer value in rhn_prefix.conf, call getInt.
# Test -10, 0, 100, y
prefix.int_minus10 = -10
prefix.int_zero = 0
prefix.int_100 = 100
prefix.int_y = y
