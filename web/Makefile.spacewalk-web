# Makefile for the backend directory
#

# global defines which control this build and where we deploy files
PREFIX	=
export PREFIX

# what are we building...
SUBDIRS	= html modules

# macros and stuff
include Makefile.defs

